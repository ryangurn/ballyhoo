source "https://rubygems.org"

# Pinned through Gemfile.lock so a release built here and a release built in CI
# come out of the same fastlane. Always invoke it as `bundle exec fastlane`;
# a stray Homebrew-installed fastlane on PATH will silently be a different one.
gem "fastlane", "~> 2.233"
